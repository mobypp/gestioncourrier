@extends('theme.admin')

@section('main-content')

    <!-- Page Heading -->
    {{-- <h1 class="h3 mb-4 text-gray-800">{{ __('Dashboard') }}</h1> --}}
    <h1 class="h3 mb-4 text-gray-800">afficher courrier</h1>

    {{-- if(auth()->check()){
        // If the user only authenticated
      } --}}
  
      {{-- $user = auth()->user(); current user data --}}
@endsection
